# /Users/adityajain/CodeSageIITD/frontend/.env.example
# Frontend environment variables for CodeSage

# Backend API URL (do not include trailing slash)
NEXT_PUBLIC_API_URL=http://localhost:8000

# WebSocket URL for real-time communication
NEXT_PUBLIC_WS_URL=ws://localhost:8000

# Optional: WebSocket path if different from /ws
NEXT_PUBLIC_WS_PATH=/ws

# Instructions:
# 1. Copy this file to .env.local: cp .env.example .env.local
# 2. Update the URLs to match your backend server
# 3. .env.local is automatically ignored by git
# 4. Use NEXT_PUBLIC_ prefix for client-side environment variables